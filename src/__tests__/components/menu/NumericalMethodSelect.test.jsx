describe('Given the component rendered', () => {
    describe('When differentiation mode seleccted', () => {
        describe('When user clicks the component', () => {
            test('Then should list differentiation methods', () => {});
        });
        describe('When user selects an option', () => {
            test('should dispatch differentiation method to calculator context', () => {});
        });
    });

    describe('When integration mode selected', () => {
        describe('When user clicks the component', () => {
            test('Then should list differentiation methods', () => {});
        });
        describe('When user selects an option', () => {
            test('should dispatch differentiation method to calculator context', () => {});
        });
    });
});
