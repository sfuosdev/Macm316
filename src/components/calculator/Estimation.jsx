import React from 'react';
import LaTex from './Latex';

function Estimation() {
    return (
        <div className="Formula">
            <LaTex tex="f(x) = e^{x}" />
        </div>
    );
}

export default Estimation;
